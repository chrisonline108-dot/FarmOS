const test=require('node:test'),assert=require('node:assert/strict'),s=require('../schedule-core.js');
test('Today, Monday-based Week, Month, Year and farm Season ranges',()=>{assert.deepEqual(s.range('Today','2026-09-21'),['2026-09-21','2026-09-21']);assert.deepEqual(s.range('Week','2026-09-23'),['2026-09-21','2026-09-27']);assert.deepEqual(s.range('Month','2028-02-15'),['2028-02-01','2028-02-29']);assert.deepEqual(s.range('Year','2026-09-21'),['2026-01-01','2026-12-31']);assert.equal(s.range('Season','2026-01-10'),null);assert.deepEqual(s.range('Season','2026-01-10',{start_month:11,start_day:1,end_month:2,end_day:28}),['2025-11-01','2026-02-28']);});
test('Recurrence clamps short months and leap years, without fabricating undated rules',()=>{assert.equal(s.occurs({recurrence:'Monthly'},'2026-09-21'),false);assert.equal(s.occurs({recurrence:'Monthly',scheduled_date:'2026-01-31'},'2026-02-28'),true);assert.equal(s.occurs({recurrence:'Yearly',scheduled_date:'2024-02-29'},'2025-02-28'),true);assert.equal(s.occurs({recurrence:'Weekly',weekday:1},'2026-09-21'),true);assert.equal(s.occurs({recurrence:'Daily',scheduled_date:'2026-09-22'},'2026-09-21'),false);assert.equal(s.occurs({recurrence:'Custom',scheduled_date:'2026-09-01',interval_days:3},'2026-09-22'),true);assert.equal(s.occurs({recurrence:'Seasonal',season_id:'season'},'2026-09-21',[{id:'season',start_month:9,start_day:21}]),true);});
test('One shared task, completion overrides only its recurrence date',()=>{const t={id:'one',title:'Fixture duty',recurrence:'Daily',status:'Open'},o=[{task_id:'one',occurrence_date:'2026-09-21',status:'Completed'}];assert.equal(s.taskEvents([t],o,'2026-09-21','2026-09-22').length,2);assert.equal(s.status(t,'2026-09-21',o),'Completed');assert.equal(s.status(t,'2026-09-22',o),'Open');assert.equal(s.status({...t,recurrence:'One-time',status:'Completed'},'2026-09-22',[]),'Completed');});
